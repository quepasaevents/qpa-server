{
  "type": "postgres",
  "host": "localhost",
  "port":  5432,
  "database": "qpa-dev",
  "entities": ["src/**/*.entity.ts"],
  "synchronize": true,
  "migrations": ["migrations/*.ts"],
  "cli": {
    "migrationsDir": "./migrations"
  },
  "logging": true
}
