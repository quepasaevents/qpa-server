{
  "type": "postgres",
  "host": "localhost",
  "database": "qpa-dev",
  "entities": ["src/**/*.entity.ts"],
  "migrations": ["migrations/*.ts"],
  "cli": {
    "migrationsDir": "./migrations"
  },
  "logging": true,
  "synchronize": true
}
