schema: src/schema.graphql
overwrite: true
generates:
  ./src/@types/index.d.ts:
    plugins:
      - add: // tslint:disable
      - typescript-common
      - typescript-server
      - typescript-mongodb
      - typescript-resolvers
