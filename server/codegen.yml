schema: src/schema.graphql
overwrite: true
generates:
  ./src/@types/graphql.d.ts:
    plugins:
      - add: // tslint:disable
      - typescript-common
      - typescript-server
      - typescript-mongodb
      - typescript-resolvers
